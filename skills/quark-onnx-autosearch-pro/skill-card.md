# Skill Card

## Description

End-to-end Quark ONNX AutoSearchPro recipe — drives `quark.onnx.AutoSearchPro` (Optuna-based hyperparameter search) on a `.onnx` model to find the best quantization config (activation/weight spec, calibration method, CLE, AdaRound / AdaQuant, FastFinetune params).

## Owner

amd (federated from [amd/Quark](https://github.com/amd/Quark))

## License

MIT
